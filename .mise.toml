[settings]
idiomatic_version_file_enable_tools = ["node"]
auto_install = false
experimental = true
lockfile = true
locked = false                                 # npm backend が checksum 未対応のため

[tools]
# このリポジトリで使うランタイム
node = "24.15.0"
pnpm = "10.33.2"

# aqua CLI（dot_config/aquaproj-aqua の管理用）
"github:aquaproj/aqua" = "2.57.2"

# このリポジトリで使う CLI ツール
"aqua:google/go-jsonnet" = "0.22.0"
"aqua:go-task/task" = "3.50.0"

# lazy-lock.yaml ワークフローで使用
"aqua:neovim/neovim" = "0.12.2"

# npm パッケージ
"npm:cc-sdd" = "3.0.2"
