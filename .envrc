# .envrc - direnv 設定
#
# このファイルはリポジトリに入った時に自動的に環境変数を設定する。
# direnv allow で有効化する必要がある。

# 親ディレクトリの .envrc があれば読み込む
source_up_if_exists

# ローカル設定（Git 管理外）があれば読み込む
source_env_if_exists .envrc.local

# mise の config ファイルを自動的に trust
# これにより task mise:lock などを実行する際に trust 確認が不要になる
# - $PWD: ルートの .mise.toml
# - $PWD/dot_config/mise: dot_config/mise/config.toml
export MISE_TRUSTED_CONFIG_PATHS="$PWD:$PWD/dot_config/mise"
